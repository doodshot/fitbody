import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1, // Riempie lo schermo
        justifyContent: "center", // Centra verticalmente
        alignItems: "center", // Centra orizzontalmente
    },
    text1: {
        fontSize: 25.47,
        fontWeight: "bold",

    }
});